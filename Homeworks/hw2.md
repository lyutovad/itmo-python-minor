# Домашняя работа 2. NumPy и базовый Pandas

**Максимум за содержание: 20 баллов**

Цель работы — закрепить:

- массивы NumPy и логические маски;
- загрузку таблиц через Pandas;
- первичный осмотр данных;
- выбор столбцов и строк;
- `loc` / `iloc`;
- фильтрацию по одному и нескольким условиям;
- `isin()` / `between()`;
- `unique()` / `nunique()` / `value_counts()`;
- сортировку.

В этой работе **не нужно** использовать `groupby`, `merge`, обработку пропусков, строк и дат — это будет позже.

---

# Формат сдачи

Работа сдаётся в формате `.ipynb`.

Имя файла:

```text
homework_2.ipynb
```

Работа выполняется в отдельной ветке:

```text
hw_2
```

Для сдачи:

1. создайте ветку `hw_2`;
2. добавьте `homework_2.ipynb`;
3. сделайте commit;
4. отправьте ветку на GitHub;
5. создайте Pull Request в `main`;
6. добавьте `@lyutovad` как reviewer.

Пример:

```bash
git commit -m "Add homework 2"
```

---

# Оформление ноутбука

Для каждого задания должен быть отдельный блок.

Перед решением добавьте Markdown-ячейку с номером и кратким описанием задания.

Код делите на логические части.

Если работа фактически выполнена одной большой кодовой ячейкой без Markdown-разделения, применяется коэффициент:

```text
итоговый балл = балл за содержание × 0.7
```

Перед сдачей выполните **Restart and Run All** и убедитесь, что ноутбук выполняется сверху вниз.

---

# Дедлайны

- **мягкий дедлайн:** 15.09.2026 23:59;
- **жёсткий дедлайн:** 22.09.2026 23:59.

После мягкого дедлайна максимальный балл составляет **50%**.

---

# Данные

Используйте файл:

```text
hw2_orders.csv
```

Файл изменять вручную не нужно.

```python
import pandas as pd

orders = pd.read_csv("hw2_orders.csv")
```

---

# Задание 1. NumPy — 4 балла

Даны массивы:

```python
import numpy as np

prices = np.array([1200, 800, 2500, 3100, 450, 1800])
counts = np.array([2, 1, 3, 1, 5, 2])
```

Выполните:

1. получите массив стоимости каждой позиции: `price * count`;
2. найдите общую стоимость всех позиций;
3. найдите среднюю цену;
4. получите только цены больше `1500`;
5. получите цены от `1000` до `3000` включительно;
6. посчитайте количество цен больше `1500`.

### Критерии

- поэлементные операции — **1 балл**;
- агрегаты — **1 балл**;
- фильтрация одной маской — **1 балл**;
- несколько условий и подсчёт по маске — **1 балл**.

---

# Задание 2. Загрузка и первичный осмотр — 4 балла

Загрузите `hw2_orders.csv` в `DataFrame`.

Получите и выведите:

1. тип объекта;
2. количество строк и столбцов;
3. названия столбцов;
4. типы данных;
5. первые 5 строк;
6. последние 3 строки;
7. `info()`;
8. `describe()`.

После кода добавьте Markdown-ячейку и коротко ответьте:

> Какие столбцы являются числовыми? Какие столбцы содержат категориальные значения?

### Критерии

- корректная загрузка — **1 балл**;
- корректный первичный осмотр — **2 балла**;
- осмысленный комментарий — **1 балл**.

---

# Задание 3. Выбор и фильтрация — 6 баллов

## 3.1. Выбор столбцов — 1 балл

Получите:

1. столбец `amount` как `Series`;
2. новый `DataFrame` только со столбцами:

```text
customer
city
category
amount
```

Проверьте тип обоих объектов.

## 3.2. Один фильтр — 2 балла

Получите:

1. только оплаченные заказы;
2. только заказы из Москвы;
3. только заказы дороже `3000`;
4. только заказы с рейтингом `5`.

## 3.3. Несколько условий — 2 балла

Получите:

1. оплаченные московские заказы дороже `2000`;
2. заказы из Казани **или** Самары;
3. заказы категории `tech` с рейтингом `5`.

## 3.4. `isin()` и `between()` — 1 балл

Одним выражением выберите заказы, для которых:

- город — Москва, Казань или Санкт-Петербург;
- сумма заказа от `1500` до `4000` включительно.

---

# Задание 4. `loc`, `iloc`, категории и сортировка — 4 балла

## 4.1. `iloc` — 1 балл

Через `iloc` получите первые 5 строк и первые 4 столбца этих строк.

## 4.2. `loc` — 1 балл

Через `loc` получите строки с индексами от `0` до `4` и только столбцы:

```text
customer
city
amount
```

## 4.3. Категории — 1 балл

Получите:

1. уникальные города;
2. количество уникальных городов;
3. количество заказов по категориям;
4. количество заказов по статусам.

## 4.4. Сортировка — 1 балл

Создайте таблицу, в которой:

1. остаются только оплаченные заказы;
2. остаются только столбцы:

```text
order_id
customer
city
category
amount
```

3. строки отсортированы по `amount` по убыванию;
4. индекс сброшен через:

```python
reset_index(drop=True)
```

Выведите первые 10 строк результата.

---

# Задание 5. Краткий вывод — 2 балла

В конце ноутбука добавьте Markdown-ячейку и коротко ответьте:

1. Чем `Series` отличается от `DataFrame`?
2. В чём разница между `loc` и `iloc`?
3. Как связаны логические маски NumPy и фильтрация Pandas?
4. Какой запрос к таблице оказался самым сложным и почему?

Достаточно нескольких содержательных предложений.

### Критерии

- ответы корректны и относятся к выполненной работе — **2 балла**.

---

# Итоговая шкала

| Задание | Баллы |
|---|---:|
| 1. NumPy | 4 |
| 2. Загрузка и осмотр | 4 |
| 3. Выбор и фильтрация | 6 |
| 4. `loc`, `iloc`, категории и сортировка | 4 |
| 5. Краткий вывод | 2 |
| **Итого** | **20** |

---

# Что не требуется

Не нужно использовать:

- `groupby`;
- `merge`;
- pivot-таблицы;
- обработку пропусков;
- даты;
- сложные строковые преобразования;
- циклы для задач, которые естественно решаются средствами NumPy / Pandas.

Цель — закрепить именно материал занятия.

# Домашняя работа 3. Pandas и визуальный анализ данных

**Максимум: 20 баллов**

В этой работе используется реальный датасет пассажиров **Titanic**.

Цель домашней работы — объединить материал нескольких последних блоков:

- преобразование столбцов;
- строковые методы Pandas;
- фильтрацию;
- исследование категориальных и числовых переменных;
- построение и выбор визуализаций;
- короткую интерпретацию результатов.

При выполнении работы ориентируйтесь на материалы семинаров.  
В заданиях намеренно не указано, какой именно метод или тип графика нужно использовать, если это является частью проверяемого навыка.

---

# Формат сдачи

Работа сдаётся в формате:

```text
homework_3.ipynb
```

Работа выполняется в отдельной ветке:

```text
hw_3
```

Для сдачи:

1. создайте ветку `hw_3`;
2. добавьте `homework_3.ipynb`;
3. добавьте файл `titanic.csv`, если он нужен для воспроизводимого запуска;
4. сделайте commit;
5. отправьте ветку на GitHub;
6. создайте Pull Request в `main`;
7. добавьте `@lyutovad` как reviewer.

---

# Оформление

Каждое задание должно начинаться с Markdown-ячейки с номером и кратким описанием.

Графики должны иметь:

- заголовок;
- понятные подписи осей;
- легенду, если она нужна.

После каждой визуализации добавьте короткий Markdown-вывод: **1–3 содержательных предложения**.

Перед сдачей выполните **Restart and Run All**.

---

# Данные

Используйте:

```text
titanic.csv
```

В датасете есть пропущенные значения. **Специально заполнять или удалять их в этой домашней работе не требуется**, если это прямо не нужно для конкретной операции.

Тему пропусков мы разберём отдельно.

---

# Задание 1. Загрузка и понимание данных — 2 балла

1. Загрузите датасет в `df`.
2. Проведите первичный анализ загруженных данных.
3. В Markdown коротко разделите признаки на:
   - категориальные;
   - числовые.

### Критерии

- корректная загрузка и осмотр — **1 балл**;
- осмысленное определение типов переменных — **1 балл**.

---

# Задание 2. Преобразования столбцов — 4 балла

## 2.1. Размер семьи — 1 балл

Создайте столбец `family_size`, равный:

```text
SibSp + Parch + 1
```

Почему прибавляется `1`? Ответьте в Markdown.

## 2.2. Стоимость билета на одного члена семьи — 1 балл

Создайте столбец `fare_per_person`, содержащий стоимость билета на одного члена семьи.

## 2.3. Работа со строковыми данными — 2 балла

Используя столбец `Name`:

1. создайте `name_length` — длину имени;
2. создайте логический столбец `has_mrs`, показывающий, содержит ли имя `"Mrs."`;
3. посчитайте количество строк, где `has_mrs == True`.

### Критерии

- `family_size` рассчитан корректно — **1 балл**;
- `fare_per_person` рассчитан корректно — **1 балл**;
- `name_length` рассчитан корректно — **1 балл**;
- `has_mrs` и количество соответствующих строк рассчитаны корректно — **1 балл**.

---

# Задание 3. Фильтрация — 4 балла

Получите и сохраните отдельные DataFrame для следующих групп.

## 3.1. — 1 балл

Женщины первого класса.

## 3.2. — 1 балл

Пассажиры:
- возраст не меньше `18`;
- стоимость билета больше `50`.

## 3.3. — 1 балл

Пассажиры, которые выжили и у которых размер семьи больше `3` человек.

## 3.4. — 1 балл

Пассажиры, в имени которых встречается `"Mrs."`, со стоимостью билета больше `30`.

Для каждого результата выведите количество найденных строк.

---

# Задание 4. Визуальный анализ — 8 баллов

Для каждого пункта:

1. выберите подходящий способ визуализации;
2. постройте график;
3. оформите его так, чтобы он был понятен без чтения кода;
4. после графика напишите **1–3 предложения с выводом**.

Тип графика нужно выбрать самостоятельно, исходя из исследовательского вопроса и типов переменных.

## 4.1. — 1 балл

Исследуйте распределение пассажиров по классам (`Pclass`).

## 4.2. — 1 балл

Исследуйте распределение возраста пассажиров (`Age`).

## 4.3. — 1 балл

Исследуйте распределение стоимости билета (`Fare`).

Обратите внимание не только на типичные значения, но и на разброс и крайние наблюдения.

## 4.4. — 1 балл

Сравните стоимость билета (`Fare`) между классами пассажиров (`Pclass`).

## 4.5. — 1 балл

Исследуйте связь между возрастом (`Age`) и стоимостью билета (`Fare`).

На одной визуализации должно быть видно, какие пассажиры выжили, а какие нет.

## 4.6. — 1 балл

Исследуйте связь между классом пассажира (`Pclass`) и выживаемостью (`Survived`).

## 4.7. Собственный исследовательский вопрос — 1 балл

Сформулируйте **один дополнительный вопрос к данным**, который можно изучить с помощью визуализации.

1. Выберите способ визуализации самостоятельно.
2. Постройте график.
3. Одной фразой объясните, почему выбран именно такой способ представления данных.
4. Напишите короткий вывод.

Не повторяйте полностью один из предыдущих пунктов задания.

## 4.8. Корреляции — 1 балл

Для признаков:

```text
Age
Fare
SibSp
Parch
family_size
```

1. посчитайте корреляции;
2. визуализируйте их;
3. укажите наиболее заметную положительную связь.

---

# Задание 5. Итоговый вывод — 2 балла

Добавьте Markdown-блок с ответами:

1. Какие 2–3 наиболее заметных наблюдения вы сделали по данным?
2. Какая визуализация оказалась наиболее полезной и почему?
3. Чем отличаются два способа анализа одной числовой переменной, которые вы использовали в задании 4?
4. Почему перед выбором визуализации важно понимать тип переменной?

Большого текста не требуется. Важны конкретные выводы по построенным графикам.

---

# Итоговая шкала

| Задание | Баллы |
|---|---:|
| 1. Загрузка и понимание данных | 2 |
| 2. Преобразования | 4 |
| 3. Фильтрация | 4 |
| 4. Визуальный анализ | 8 |
| 5. Итоговый вывод | 2 |
| **Итого** | **20** |

---

# Что не требуется

В этой работе не требуется:

- заполнять пропуски;
- удалять выбросы;
- использовать `groupby`;
- использовать `merge`;
- строить интерактивные графики;
- использовать Plotly;
- применять методы машинного обучения.

Эти темы либо будут разобраны позже, либо выходят за рамки текущей домашней работы.

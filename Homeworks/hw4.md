# Домашняя работа 4. Визуальный анализ и качество данных

**Максимум: 20 баллов**

Цель работы — научиться работать с реальным датасетом не только на уровне отдельных команд Pandas, а как с данными, которые нужно сначала понять и проверить.

В этой работе нужно:

- провести первичный анализ датасета;
- самостоятельно выбирать подходящие визуализации;
- исследовать пропущенные значения;
- проверить дубликаты;
- найти и проанализировать выбросы;
- делать содержательные выводы по результатам анализа.

## Датасет

Используем реальные данные об объявлениях Airbnb в Нью-Йорке из проекта **Inside Airbnb**.

Источник:

https://insideairbnb.com/get-the-data/

Для работы используйте **одну фиксированную версию файла `listings.csv`**, приложенную к домашней работе.  
Не скачивайте более новую версию вместо приложенного файла: состав данных со временем меняется, и результаты могут отличаться.

Перед началом работы откройте описание источника и посмотрите, что означают основные поля датасета.

---

## Формат сдачи

Работа сдаётся в формате `.ipynb`.

Имя файла:

```text
homework_4.ipynb
```

Работа выполняется в отдельной ветке:

```text
hw_4
```

В Pull Request должен быть добавлен преподаватель `@lyutovad` как reviewer.

Перед сдачей выполните:

```text
Restart and Run All
```

Ноутбук должен выполняться сверху вниз без ошибок.

Все графики должны иметь понятные подписи. После каждого содержательного блока должен быть короткий вывод в Markdown.

---

# 1. Загрузка и знакомство с данными — 2 балла

Загрузите датасет в `df`.

Проведите первичный осмотр:

- размер таблицы;
- названия столбцов;
- типы данных;
- несколько первых строк;
- базовая информация о числовых признаках.

В Markdown коротко ответьте:

1. Что является одной строкой датасета?
2. Назовите не менее трех числовых и трех категориальных признаков.
3. Какие 3–5 столбцов кажутся вам наиболее полезными для анализа объявлений Airbnb и почему?

---

# 2. Пропущенные значения — 5 баллов

## 2.1. Найдите пропуски — 2 балла

Для каждого столбца посчитайте:

- количество пропущенных значений;
- долю пропусков в процентах.

Покажите только столбцы, в которых есть пропуски, в порядке убывания их количества.

Ответьте:

- в каких столбцах пропусков больше всего;
- одинаково ли критичны пропуски в разных столбцах.

## 2.2. Исследуйте причину пропусков — 1 балл

Отдельно исследуйте `reviews_per_month`.

Проверьте, связаны ли пропуски в этом столбце со значением `number_of_reviews`.

Не заменяйте пропуски сразу. Сначала сформулируйте вывод по данным.

## 2.3. Выберите стратегию обработки — 2 балла

Выберите **не менее двух столбцов с пропусками**.

Для каждого:

1. предложите способ обработки;
2. объясните, почему выбрали именно его;
3. примените выбранное решение.

Допустимы разные решения, если они обоснованы данными.

---

# 3. Визуальный анализ — 6 баллов

Для каждого вопроса самостоятельно выберите подходящий тип графика.

Не нужно повторять все виды графиков из лекции. Выбирайте визуализацию исходя из типа переменных и исследовательского вопроса.

Каждый график должен содержать:

- заголовок;
- понятные подписи осей;
- единицы измерения, если они нужны;
- короткий вывод в Markdown.

## 3.1. Цена — 1.5 балла

Исследуйте распределение `price`.

По графику ответьте:

- где находится основная масса значений;
- симметрично ли распределение;
- есть ли значения, сильно отличающиеся от большинства.

## 3.2. Тип жилья — 1.5 балла

Покажите, как распределены объявления по `room_type`.

Ответьте:

- какой тип встречается чаще всего;
- есть ли редкие категории.

## 3.3. Цена и тип жилья — 1.5 балла

Сравните `price` между различными `room_type`.

Выберите график, на котором удобно сравнивать не только одно среднее значение, но и распределение цен внутри групп.

Сделайте содержательный вывод.

## 3.4. Отзывы — 1.5 балла

Исследуйте связь между двумя числовыми признаками, связанными с отзывами.

Например, можно использовать:

- `number_of_reviews`;
- `reviews_per_month`.

Самостоятельно выберите визуализацию и объясните, что можно сказать о связи признаков.

---

# 4. Дубликаты — 2 балла

Проверьте:

1. есть ли в таблице полные дубликаты строк;
2. есть ли повторяющиеся значения идентификатора объявления `id`.

Выведите количество найденных повторов.

В Markdown ответьте:

- одинаковы ли понятия «полный дубль строки» и «повторяющийся ID»;
- нужно ли что-либо удалять в этом датасете по результатам вашей проверки.


---

# 5. Выбросы — 4 балла

## 5.1. Исследование `price` — 2 балла

Исследуйте возможные выбросы в `price`.

Используйте:

- визуальный анализ;
- метод IQR.

Посчитайте:

- нижнюю границу;
- верхнюю границу;
- количество значений за пределами границ;
- долю таких значений от всего датасета.

## 5.2. Интерпретация — 1 балл

Посмотрите несколько самых больших значений `price`.

Ответьте:

- являются ли они обязательно ошибками;
- можно ли удалить все значения, определённые IQR как выбросы, только на основании этого правила.

## 5.3. Ещё один признак — 1 балл

Выберите самостоятельно ещё один числовой признак, в котором могут быть необычные значения.

Исследуйте его визуально и кратко объясните:

- есть ли потенциальные выбросы;
- являются ли они ошибкой или могут быть реальными наблюдениями.

---

# 6. Итог — 1 балл

В конце ноутбука напишите **3–5 содержательных выводов** по работе.

В выводах должны быть отражены как минимум:

- один результат визуального анализа;
- один вывод о пропусках;
- один вывод о выбросах или качестве данных.

Не перечисляйте использованные функции. Пишите выводы именно о данных.

---

# Что в этой работе делать не нужно

В этой домашней работе **не требуется**:

- кодировать категориальные признаки;
- применять `get_dummies` / One-Hot Encoding;
- масштабировать признаки;
- делить данные на train / test;
- обучать ML-модель;
- автоматически удалять все найденные выбросы.

Эти этапы будут разбираться отдельно.

---

# Критерии оценивания

| Блок | Баллы |
|---|---:|
| Загрузка и знакомство с данными | 2 |
| Пропущенные значения | 5 |
| Визуальный анализ | 6 |
| Дубликаты | 2 |
| Выбросы | 4 |
| Итоговые выводы | 1 |
| **Итого** | **20** |

Работа оценивается не только по наличию кода.  
Для заданий на анализ обязательны содержательные выводы в Markdown.

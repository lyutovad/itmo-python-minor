# Домашняя работа 5. Преобразование и подготовка признаков

**Максимум: 20 баллов**

Это финальная домашняя работа курса.

Она продолжает работу с реальным датасетом Airbnb из предыдущей домашней работы, но фокус теперь другой: не исследование качества данных, а **преобразование признаков**.

Используйте тот же фиксированный файл:

```text
listings.csv
```

Работа должна быть самостоятельной: даже если вы уже исследовали этот датасет в HW4, начните с загрузки исходного файла.

В этой работе нужно применить:

- векторные преобразования;
- `.str`;
- `map`;
- `apply`;
- `lambda`;
- `loc`;
- перекодирование категорий;
- масштабирование числовых признаков.

---

## Формат сдачи

Файл:

```text
homework_5.ipynb
```

Ветка:

```text
hw_5
```

Перед сдачей:

```text
Restart and Run All
```

Ноутбук должен выполняться сверху вниз без ошибок.

После содержательных блоков пишите короткие выводы в Markdown.

---

# 1. Подготовка рабочей таблицы — 1 балл

Загрузите `listings.csv` в `df`.

Создайте отдельную таблицу `df_work`, оставив следующие столбцы:

```text
id
name
neighbourhood_group
room_type
price
minimum_nights
number_of_reviews
reviews_per_month
availability_365
```

Проверьте:

- размер `df_work`;
- типы столбцов;
- пропуски в выбранных признаках.

Для дальнейших заданий удалите строки, где отсутствует `price` или `minimum_nights`.

Исходный `df` не изменяйте.

---

# 2. Векторные и строковые преобразования — 3 балла

## 2.1. Векторная операция — 1 балл

Создайте признак:

```text
price_per_week = price * 7
```

Используйте операцию над целым столбцом, без цикла и `apply`.

## 2.2. Строковый признак — 1 балл

Создайте:

```text
name_length
```

— длину названия объявления.

Используйте строковые возможности Pandas.

Если `name` отсутствует, результат может остаться пропуском.

## 2.3. Короткий вывод — 1 балл

Объясните:

- почему для этих двух задач не нужен `apply`;
- какой способ здесь проще и естественнее.

---

# 3. `map`, словарь и ручное перекодирование — 3 балла

## 3.1. Сокращение районов — 1 балл

Создайте словарь:

```text
Manhattan -> MAN
Brooklyn -> BRK
Queens -> QNS
Bronx -> BRX
Staten Island -> SI
```

С помощью `map` создайте столбец:

```text
borough_code
```

Проверьте результат через `value_counts()`.

## 3.2. Бинарный признак — 1 балл

Создайте столбец:

```text
is_entire_home
```

где:

- `1` — `room_type == "Entire home/apt"`;
- `0` — остальные типы жилья.

Можно использовать словарь или другой понятный способ.

## 3.3. Почему не Label Encoding? — 1 балл

Ответьте в Markdown:

Почему кодирование

```text
Manhattan = 0
Brooklyn = 1
Queens = 2
...
```

может быть плохой идеей для ML-модели?

Какой способ кодирования лучше подходит для `neighbourhood_group`, если между районами нет естественного порядка?

---

# 4. `loc`: условное преобразование — 2 балла

Создайте столбец:

```text
stay_type
```

По значению `minimum_nights`:

- меньше `7` → `"short"`;
- от `7` до `30` включительно → `"medium"`;
- больше `30` → `"long"`.

Сделайте преобразование через `loc`.

После этого:

1. покажите `value_counts()`;
2. проверьте несколько строк каждой категории;
3. объясните, почему здесь `loc` удобнее, чем изменение таблицы в цикле.

---

# 5. `apply` и `lambda` — 3 балла

## 5.1. Функция по нескольким столбцам — 2 балла

Создайте признак:

```text
review_activity
```

Правила:

- если `number_of_reviews == 0` → `"no_reviews"`;
- иначе если `reviews_per_month >= 2` → `"active"`;
- иначе → `"rare"`.

Логика зависит сразу от двух столбцов, поэтому используйте отдельную функцию и:

```python
df_work.apply(..., axis=1)
```

Если `reviews_per_month` пропущен, но `number_of_reviews > 0`, отнесите объект к `"rare"`.

## 5.2. `lambda` — 1 балл

Создайте ещё один небольшой признак через `lambda`.

Он должен быть осмысленным и использоваться только один раз.

В Markdown напишите, почему для этой задачи короткая `lambda` была уместна.

---

# 6. Кодирование номинальной категории — 2 балла

Преобразуйте `room_type` с помощью One-Hot Encoding.

Можно использовать:

```python
pd.get_dummies(...)
```

Требования:

1. создайте dummy-столбцы;
2. добавьте их к `df_work` или создайте отдельную таблицу;
3. покажите первые строки;
4. объясните, почему One-Hot Encoding здесь лучше простого присвоения чисел `0, 1, 2, ...`.

---

# 7. Масштабирование — 4 балла

Выберите признаки:

```text
price
minimum_nights
number_of_reviews
availability_365
```

## 7.1. `StandardScaler` — 2 балла

Создайте копию выбранных признаков и примените `StandardScaler`.

Сравните `describe()` до и после масштабирования.

Ответьте:

- что произошло со средними значениями;
- что произошло со стандартными отклонениями;
- сохранился ли порядок объектов внутри каждого признака.

## 7.2. `MinMaxScaler` — 1 балл

Для тех же признаков отдельно примените `MinMaxScaler`.

Проверьте минимальные и максимальные значения после преобразования.

## 7.3. Сравнение — 1 балл

В Markdown кратко объясните разницу между:

- `StandardScaler`;
- `MinMaxScaler`.

---

# 8. Финальная таблица и вывод — 2 балла

Соберите итоговую таблицу признаков, в которой:

- нет исходных текстовых `room_type` и `neighbourhood_group`;
- есть созданные вами числовые / закодированные признаки;
- сохраняется `id` как идентификатор объекта.

Не нужно обучать модель.

В конце напишите 3–5 предложений:

1. какие преобразования были векторными;
2. где понадобился `map`;
3. где действительно был нужен `apply`;
4. почему `lambda` не стоит использовать для каждой операции;
5. зачем масштабируют числовые признаки.

---

# Что делать не нужно

В этой работе **не требуется**:

- train/test split;
- обучение ML-модели;
- подбор алгоритма;
- оценка качества модели.

Эти темы относятся уже к следующему курсу по машинному обучению.

---

# Критерии оценивания

| Блок | Баллы |
|---|---:|
| Подготовка рабочей таблицы | 1 |
| Векторные и строковые преобразования | 3 |
| `map` и ручное перекодирование | 3 |
| `loc` | 2 |
| `apply` и `lambda` | 3 |
| One-Hot Encoding | 2 |
| Масштабирование | 4 |
| Финальная таблица и вывод | 2 |
| **Итого** | **20** |
